export interface thisSource {
  id: string;
  name: string;
}

export interface IPosts {
  nickname: string;
  value: string;
}

export interface userInfo {
  id: string;
  email: string;
  avatarUrl: string;
  nickname: string;
}
